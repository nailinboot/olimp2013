#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
	freopen("PARQUET.IN", "r",stdin);
	
	int t,n;
	cin >> t;
	for (int i=1; i<=t; i++)
	{
		cin >> n;
		if ((n==3) || (n==4) || (n==6)) cout << "YES" << endl;
		 else cout << "NO" << endl;
	}	
		
	return 0;
}
